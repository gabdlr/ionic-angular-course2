import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ng.pairbnb.course',
  appName: 'ionic-angular-course2',
  webDir: 'www',
  server: {
    androidScheme: 'https',
  },
};

export default config;
